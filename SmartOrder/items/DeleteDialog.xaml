<Window x:Class="SmartOrder.DeleteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Διαγραφή" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        >
    <Grid Width="{DynamicResource DialogGridWidth}" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Label Name="lblCategory" Grid.Row="0" Grid.Column="0" Content="Είσαι σίγουρος ;" />
        <WrapPanel Grid.Row="2" HorizontalAlignment="Right" Margin="10">
            <Button IsDefault="True" Name="btnDialogOk" Click="btnDialogOk_Click" MinWidth="60" Margin="0,0,10,0" Content="_OK" />
            <Button IsCancel="True" MinWidth="60" Click="Button_Click"  Content="_Άκυρο" />
        </WrapPanel>
    </Grid>
</Window>
